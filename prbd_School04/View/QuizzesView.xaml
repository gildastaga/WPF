<f:UserControlBase x:Class="School04.View.QuizzesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
             xmlns:local="clr-namespace:School04.View"
             xmlns:vm="clr-namespace:School04.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="wrapColumn">
            <Setter Property="TextBlock.TextWrapping" Value="Wrap" />
        </Style>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:QuizzesViewModel x:Name="vm" />
    </UserControl.DataContext>
    <StackPanel>
        <f:MyDataGrid x:Name="grid" ItemsSource="{Binding MessagesView}"
                  AutoGenerateColumns="False" FontSize="12" IsReadOnly="False" Margin="5" CanUserAddRows="False" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" MaxHeight="160"
                  SelectedItemsList="{Binding SelectedItems}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Path=DateTime}" IsReadOnly="True" SortDirection="Descending" Width="auto" MinWidth="80"/>
                <DataGridTextColumn Header="Start" Binding="{Binding Path=Author.Pseudo, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" IsReadOnly="True" Width="auto" MinWidth="80" />
                <DataGridTextColumn Header="Finish" Binding="{Binding Path=Author.Pseudo, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" IsReadOnly="True" Width="auto" MinWidth="80" />
                <DataGridTextColumn Header="# Questions" Binding="{Binding Path=IsPrivate}" IsReadOnly="True"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <KeyBinding Key="Delete" Command="{Binding DeleteMessages}" CommandParameter="{Binding SelectedItemsList, ElementName=grid}" />
            </DataGrid.InputBindings>
        </f:MyDataGrid>
        <Button HorizontalAlignment="Right" Content="New Quiz" Margin="5" Command="{Binding DeleteMessages}" CommandParameter="{Binding SelectedItemsList, ElementName=grid}"/>
    </StackPanel>
</f:UserControlBase>