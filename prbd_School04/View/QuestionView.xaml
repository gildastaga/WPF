<f:UserControlBase x:Class="School04.View.QuestionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:School04.View"
      xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer>

        <Grid MinHeight="900">

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="Black" BorderThickness="3" Grid.ColumnSpan="2" Width="550">
                <StackPanel Margin="10" Width="520" Grid.ColumnSpan="2">
                    <DockPanel  HorizontalAlignment="Center" >
                        <TextBlock Text="Question" Background="AliceBlue" Width="100" FontSize="25" Foreground="Blue"/>
                    </DockPanel>
                    <DockPanel  HorizontalAlignment="Center" >
                        <TextBlock Text="Que signifie PHP?" Background="AliceBlue" Width="1000" FontSize="25" Foreground="Black" Margin="20" Padding="5"/>
                    </DockPanel>

                    <Label Visibility="{Binding Visible, UpdateSourceTrigger=PropertyChanged}" Content="Propositions :" Grid.Row="4" />
                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="4">
                        <ListView ScrollViewer.VerticalScrollBarVisibility="Disabled" BorderBrush="Transparent" ItemsSource="{Binding Propositions}"  SelectionMode="Single">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="5"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding Checked}" Command="{Binding CheckProposition, ElementName=root}" CommandParameter="{Binding}" Margin="10"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                  
                    <DockPanel Grid.Row="3" >
                        <TextBlock Height="50" 
                       TextWrapping="Wrap" 
                       TextAlignment="Center"
                       HorizontalAlignment="Center" 
                       Width="220" 
                       Margin="20">
                       <Hyperlink Command="{Binding Create}"> <Run Text="Publish Your Question"/> </Hyperlink>
                        </TextBlock>
                    </DockPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</f:UserControlBase>
