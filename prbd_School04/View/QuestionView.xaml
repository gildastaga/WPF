<f:UserControlBase x:Class="School04.View.QuestionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:School04.View"
      xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer>

        <Grid MinHeight="900">

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="Black" BorderThickness="3" Grid.ColumnSpan="2" Width="550">
                <StackPanel Margin="10" Width="520" Grid.ColumnSpan="2">

                <Label Visibility="{Binding Visible, UpdateSourceTrigger=PropertyChanged}" Content="Categories :" Grid.Row="4" />
                <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="4">
                    <ListView ScrollViewer.VerticalScrollBarVisibility="Disabled" BorderBrush="Transparent" ItemsSource="{Binding Tags}"  SelectionMode="Single">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="5"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding Checked}" Command="{Binding CheckTag, ElementName=root}" CommandParameter="{Binding}" Margin="10"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                        <Button x:Name="All"  Content="All" HorizontalAlignment="Left"   Command="{Binding Save}" VerticalAlignment="Top" Width="75"/>
                        <Button x:Name="None"  Content="None" HorizontalAlignment="Left" Margin="20, 0, 0, 0"   Command="{Binding Save}" VerticalAlignment="Top" Width="75"/>
                    </StackPanel>

                <Label Content="Available Questions(5/5)" 
                    Grid.Row="2"
                    VerticalAlignment="Center" Padding="10"/>

                    <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="4">
                        <Button x:Name="newQuestion"  Content="NewQuestion" HorizontalAlignment="Left"   Command="{Binding NewQuestion}" VerticalAlignment="Top" Width="75"/>
                        <Button x:Name="save"  Content="Save" HorizontalAlignment="Left"   Command="{Binding Save}"  Margin="20, 0, 0, 0" VerticalAlignment="Top" Width="75"/>
                        <Button x:Name="cancel"  Content="Cancel" HorizontalAlignment="Left"   Command="{Binding Cancel}"  Margin="20, 0, 0, 0" VerticalAlignment="Top" Width="75"/>
                        <Button x:Name="delete"  Content="Delete" HorizontalAlignment="Left"   Command="{Binding Delete}"  Margin="20, 0, 0, 0" VerticalAlignment="Top" Width="75"/>
                    </StackPanel>

                </StackPanel>
                
            </Border>
        </Grid>
    </ScrollViewer>
</f:UserControlBase>
